<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.best.love_parttimejob_app" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="LoginActivity"
            android:theme="@style/StopTitle" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ValidateLoginActivity"
            android:label="@string/title_activity_validate"
            android:theme="@style/StopTitle" >
        </activity>
        <activity
            android:name=".RegisteredActivity"
            android:label="@string/title_activity_registered"
            android:theme="@style/StopTitle">
        </activity>
    </application>

</manifest>
